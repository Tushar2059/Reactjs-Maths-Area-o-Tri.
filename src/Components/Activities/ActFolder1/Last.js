import React from 'react'
import { Outlet } from 'react-router-dom'
import Line from '../Line'

const Last = () => {
  return (
    <div className='' style={{height:"100%"}}>
        {/* {flag ? <Outlet /> : <Line  color={"black"} ></Line>}   */}
        {/* <Line/> */}
    </div>
  )
}

export default Last